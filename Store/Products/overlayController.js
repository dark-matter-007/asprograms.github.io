function showImageOverlay() {
  const myOverlay = document.getElementById("imageOverlay");
  myOverlay.className = "visible";
}

function hideImageOverlay() {
  const myOverlay = document.getElementById("imageOverlay");
  myOverlay.className = "hidden";
}

showImageOverlay();
// hideImageOverlay();
